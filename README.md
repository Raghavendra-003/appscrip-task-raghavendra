# Appscrip-task-YourName

Product Listing Page (PLP) — Next.js (SSR) implementation.

## Run locally
1. npm install
2. npm run dev
3. Open http://localhost:3000

## Notes
- Replace `yourdomain.com` in JSON-LD with your deployed domain before submission.
